<div class="ui segment">
  Recent Post Widget
  <div class="ui list">
    {% for post in posts %}
      <div class="item">
        <a href="/{{ post.getCategory() }}/@{{ post.author }}/{{ post.permlink }}">
          {{ post.title }}
        </a>
      </div>
    {% endfor %}
  </div>
</div>
